<%= form_for(@infomation) do |f| %>
  <% if @infomation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@infomation.errors.count, "error") %> prohibited this infomation from being saved:</h2>

      <ul>
      <% @infomation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
